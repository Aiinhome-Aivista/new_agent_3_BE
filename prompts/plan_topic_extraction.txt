Below is a Knowledge Transfer plan. Extract every individual topic/session line item listed in its schedule or breakdown (these could be in tables, bulleted lists, or numbered lists under Day headings).

Plan content:
{generated_content}

Return ONLY a JSON array of objects, each with keys:
- "day_label" (string, e.g. "Day 1: Python Fundamentals and Core Concepts" — the section heading this topic falls under, or "General" if there are no day sections)
- "topic_name" (string, the topic/row name, e.g. "Data Types and Variables")
- "estimated_duration_hours" (string, e.g. "1" — use "N/A" if not specified, or parse from e.g. "30 minutes" to "0.5")

Do not include any explanation, only the JSON array.
